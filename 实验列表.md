# WTA 分区有效性实验列表

最后更新：2026-07-06（P0-1/2/5/8 medium seed0 **PASS**；待 P0-3 seed1/2 + P0-4 full）

## 0. 规模速查：medium 还是 full？

| 层级 | 需要什么规模 | 说明 |
| --- | --- | --- |
| **判生死（继续/放弃 claim）** | **mostly medium** | P0 除 #4 外均可 medium；#1/#2/#5/#8 可后验、无需重训 |
| **写主文机制段** | **medium P0 + P0-4 full** | full 只集中跑 **P0-4** 即可，不必每项都 full |
| **P1 机制加深** | medium 优先 | threshold sweep / emergence 用 medium |
| **P2 应用（SDPM/readout）** | mostly full | 不阻塞 WTA 分区生死线 |

---

## 0.1 P0 medium seed0 最新结果（2026-07-06，服务器）

| 实验 ID | 结果 | 判据 |
| --- | --- | --- |
| P0-1 random stable-only (k=61, 10 seeds) | **52.06% ± 6.09 pp** vs WTA 77.50% | ✅ **PASS** |
| P0-5 random mask-stable (k=61, 10 seeds) | **70.19% ± 2.94 pp** vs mask WTA stable 19.30% | ✅ **PASS** |
| P0-2 shuffled history stable-only | 64.3% / 49.3% / 55.9% | ✅ **PASS** |
| P0-8 frequency-only | 77.50%（= WTA） | ⚠️ **NARROW** — claim 收窄为 winner-frequency |
| P0-3 multi-seed | seed0 ✅ | 🟡 seed 1/2 待跑 |
| P0-4 full | — | ⬜ 待跑 |

产物：`experiments/server_partition_group_diagnosis_medium_seed0/diagnostics/partition_counterfactuals.json`（本机镜像）  
精简 JSON：`published_results/diagnostics/partition_counterfactuals_medium_seed0.json`

---

## 1. 这份列表够不够证明「WTA 分区有效」？

**结论：方向正确，P0 框架基本够用；补 3 条对照 + 明确判据后，可以支撑「是否继续这条 claim」的决策。**

你要证明的不是「WTA 本身」，而是：

> **Task1 期间的 WTA 竞争历史，能在 Task1 结束后识别出功能上不同的神经元子集；其中 stable 子集在 Task1 推理中不可替代，shared/reserve 不是随机分组。**

这属于 **机制诊断 + 反事实 falsification**，不是单纯再刷一个 accuracy 表格。你现在的 P0 正好对准这一点。

### 已有正证据（seed 0, medium）

| 现象 | 数值 | 含义 |
| --- | ---: | --- |
| stable-only ≈ all-200 | 76.4% vs 76.5% | stable 子集几乎承载全部 Task1 推理 |
| mask-stable 崩 | 25.4% | 去掉 stable 后 Task1 几乎不可用 |
| natural WTA → stable | 97.8% | 测试竞争也高度集中在 stable |
| shared/reserve-only | 26.6% / 10.1% | 非 stable 池单独推理接近 chance |

**但这只说明「当前规则切出来的 stable 有用」，还不能说明「必须是 WTA 历史切出来的 stable 才有用」。**  
P0 的反事实实验正是为了补这一刀。

### 审阅意见：优先级是否合理？

**总体：P0 > P1 > P2 的排序没问题。**  
reserve / SDPM / full readout 放在后面也合理——它们验证的是「分区能不能拿来用」，不是「分区是不是 WTA 历史驱动的真实结构」。

建议的 **微调**（不改变大框架）：

| 原序号 | 建议 | 理由 |
| ---: | --- | --- |
| 1 与 5 | **保持 1 在前，但 5 要与 1 联读** | 若 random mask 也崩，说明网络整体冗余低；此时 random stable-only 若也接近 all，不能单独证伪 WTA，需看 per-class 覆盖 |
| 3 | **维持 P0** | 多 seed 是稳定性底线，不能降 |
| 4 | **维持 P0，但可并行启动** | full 耗时长，可与 1/2/3 并行，不必等 medium 全跑完 |
| P1 #6 | **建议提前到 P0 末尾或 P1 最前** | random 61 神经元是否覆盖 10 类，会直接影响如何解读 #1 和 #5 |
| P1 #8 | **与「最短必跑 #5」合并理解** | frequency-only 是算法简化生死线，优先级够高 |

### 建议补充的 3 条（不推翻原表，而是让 P0 更可判）

| 补充项 | 放在哪 | 证明什么 | 不做会怎样 |
| --- | --- | --- | --- |
| **A. per-class stable 覆盖** | P0 末尾 / P1 最前 | 10 类是否都有 stable winner；random 对照是否因缺类而失败 | random 61 失败可能是「没覆盖 10 类」，不是 WTA 无效 |
| **B. matched-active random** | P0 #1 的子对照 | 从 **f_i>0 的 active 神经元** 中随机抽 k 个，而非从 200 个里盲抽 | 避免 random 抽到 dead/极低频神经元造成假阴性 |
| **C. 双 shuffle 语义写清** | P0 #2 细化 | **C1**：只打乱 neuron↔history 对应（permute rows）；**C2**：保留 f_i 排名、打乱 label 分布 | 两种 null hypothesis 不同，只做一个可能漏判 |

---

## 2. 明确判据：什么叫「WTA 分区 claim 站稳 / 倒掉」

建议在每条实验后预先写 **pass / fail / ambiguous**，避免跑完后主观解释。

### P0 总判据（五项最短必跑组合）

**Claim 站稳（可进入 P1 写机制）需同时满足：**

1. **WTA stable-only** 相对 all-200：|Δ| ≤ **2 pp**（medium/full 各自阈值可写 1–2 pp）
2. **random stable-only（matched-active, 10 seeds）** 相对 all-200：平均 Δ ≤ **-5 pp**，且 **显著低于 WTA stable-only**（≥ 3 pp gap）
3. **shuffled-history partition** 后：stable-only 不再 ≈ all，或 mask-stable 不再极端崩（至少一项明显失效）
4. **seed 0/1/2** 上 1–2 现象方向一致（不要求数值完全相同）
5. **full Task1** 上 stable 主导仍成立（stable-only ≈ all；mask-stable 大掉点）
6. **frequency-only 分区** 若与 WTA 分区结论几乎相同 → **claim 收窄**为「winner-frequency partition」，不是失败，但算法要简化

**Claim 倒掉（应舍弃或重做分区算法）若出现任一：**

- matched-active random k 神经元也能 stable-only ≈ all（与 WTA stable 差距 < 2 pp）
- shuffled history 后现象不变
- 仅 seed 0 成立，seed 1/2 反向
- full 上 stable 不主导（natural WTA stable 占比 < 80%，或 stable-only 掉点 > 5 pp）
- random mask 与 mask-stable 同样崩，且 **per-class 覆盖显示 stable 只是「覆盖了更多类」** 而非 WTA 历史特异

**Ambiguous（需加实验，不能写进主文）：**

- random 崩、WTA stable 好，但 per-class 显示 stable 只是每类都有代表 → 可能有效，但 claim 应改成「class-balanced high-frequency subset」
- medium 成立、full 不成立 → 只能写 exploratory / appendix

---

## 3. 实验总表

排序原则：**先证伪/证实 WTA 分区机制；再决定 claim 能写多强；最后才做应用验证。**

### P0 — 生死实验（先跑）

| 优先级 | 实验 ID | 实验名称 | 规模 | 要证明 / 证伪什么 | 若失败意味着什么 | 状态 |
| ---: | --- | --- | --- | --- | --- | --- |
| 1 | P0-1 | **random same-size stable-only / mask** | medium 后验 | 随机 k 个是否也能 only≈all | random ≈ all → claim 倒 | ✅ 52% vs 77.5% |
| 1b | P0-1b | **matched-active random** | medium 后验 | 只在 f_i>0 池随机抽 k | 排除 dead 假阴性 | ✅（dead=0，同 P0-1） |
| 2 | P0-2 | **winner-history shuffled partition** | medium 后验 | 打乱 history 后是否失效 | 打乱仍有效 → 非 history 驱动 | ✅ 64/49/56% |
| 2a | P0-2a | shuffle C1：permute rows | medium 后验 | 破坏 neuron↔history 对应 | 同上 | ✅ |
| 2b | P0-2b | shuffle C2：固定 f_i、打乱 label | medium 后验 | 高频 vs 选择性 | 区分 null hypothesis | ⬜ 可选 |
| 3 | P0-3 | **multi-seed medium diagnosis** | medium 重训 | seed 0/1/2 方向一致 | 仅 seed0 → 不能主 claim | 🟡 seed0 |
| 4 | P0-4 | **full-scale partition diagnosis** | **full 重训** | full Task1 后 stable 仍主导 | full 不成立 → 只能 appendix | ⬜ |
| 5 | P0-5 | **random mask-stable 对照** | medium 后验 | 随机 mask k 是否也崩 | 随机也崩 → 冗余低；联读 #1 | ✅ 70% vs mask WTA 19% |
| 6 | P0-6 | **per-class stable coverage** | medium 后验 | 每类是否有 stable；random 是否缺类 | 只覆盖少数类 → 改叙事 | ⬜ |

**P0 最短必跑组合（5+1 项）：** P0-1（含 1b）→ P0-2 → P0-3 → P0-4 → P0-8（见 P1）→ 建议加 P0-6。

---

### P1 — 强证据实验（决定 claim 能写多强）

| 优先级 | 实验 ID | 实验名称 | 要证明 / 证伪什么 | 若失败意味着什么 | 状态 |
| ---: | --- | --- | --- | --- | --- |
| 6 | P1-6 | **per-class stable coverage**（若未在 P0 做） | 同上 | 同上 | ⬜ |
| 7 | P1-7 | **stable selectivity / purity** | stable 是否高频 winning 且对 Task1 类别 selective | 只高响应不 selective → 解释变弱 | ⬜ |
| 8 | P1-8 | **frequency-only / selectivity-only / combo 消融** | 当前规则是否必须 f+q 组合 | frequency-only 就够 → 算法简化 | ⚠️ medium 上 f-only = WTA |
| 9 | P1-9 | **threshold sensitivity** | 改 f_stable_percentile、q_min 后结论是否稳定 | 稍变就失效 → 调参产物 | ⬜ |
| 10 | P1-10 | **training-time emergence** | stable 是否随 Task1 训练逐渐形成并承担推理 | 后验偶然切出 → 机制说法变弱 | ⬜ |

**P1-7 建议指标：**

- stable 的 mean f_i、mean q_i vs shared/reserve
- stable 内 dominant_label 与 decision_map 一致比例
- 每类 top-winner 中 stable 占比

**P1-9 建议 sweep：**

- `f_stable_percentile` ∈ {0.60, 0.70, 0.80}
- `q_stable_min` ∈ {0.50, 0.60, 0.70}
- 记录 stable 数量与 P0 核心 contrast 是否方向不变

**P1-10 建议做法：**

- Task1 S3 epoch ∈ {10, 25, 50, 100}（medium）或 checkpoint 序列
- 每个 checkpoint 做 lightweight 版 P0-3（至少 all / stable-only / mask-stable）

---

### P2 — 应用验证（不是 WTA 分区生死线）

| 优先级 | 实验 ID | 实验名称 | 作用 | 状态 |
| ---: | --- | --- | --- | --- |
| 11 | P2-11 | **task-aware readout full 复验** | 分区/label counts 能否改进读出（需声明 task-aware 设定） | 🟡 medium 完成 |
| 12 | P2-12 | **SDPM random protection vs WTA protection** | occupancy 软保护是否优于随机少更新 | ⬜ |
| 13 | P2-13 | **hard-freeze stable vs SDPM** | 回答「是否等价于冻结旧神经元」 | ⬜ |
| 14 | P2-14 | **SDPM multi-seed** | 加强应用结果，非分区核心证据 | ⬜ |
| 15 | P2-15 | **reserve 修正 / random reserve / Task2 reserve win rate** | 验证容量分配；当前 reserve 干扰主线，暂后置 | ⬜ |

---

## 4. 当前证据在列表中的位置

| 已有结果 | 对应实验 | 定位 |
| --- | --- | --- |
| stable-only≈all, mask-stable 崩, 97.8% stable wins | P0-3 seed0 | ✅ 正证据 |
| P0 random / shuffle / mask 对照 | P0-1/2/5 | ✅ **PASS**（medium seed0） |
| frequency-only = WTA | P0-8 / P1-8 | ⚠️ claim 收窄 |
| logging+partition no-op | 非本列表主实验 | 说明分区不改变训练行为，支持「功能意义在读出/后续使用」 |
| task-aware readout +4.1 / +3.3 pp | P2-11 medium | **应用层正证据**，不能替代 P0 反事实 |
| SDPM-only full +9 pp Task1 retention | P2-14 相关 | **SDPM 有效**，不能证明 WTA 分区规则正确 |
| reserve Task2 掉点 | P2-15 | 暂不阻塞 WTA 分区生死线 |

**现在缺的两句话：**

1. 多 seed 行（P0-3 seed 1/2）  
2. full 也行（P0-4）

「随机不行」「打乱不行」已在 medium seed0 上 **PASS**。

---

## 5. 跑完 P0 后怎么改论文叙事

| 结果模式 | 建议叙事 |
| --- | --- |
| P0 全支持 | 「WTA 竞争历史诱导 experience-dependent functional partition；stable pool carries Task1 inference」 |
| random/shuffled 也支持 | **放弃当前 stable/shared/reserve 算法**；回到 winner-frequency 或重做分区 |
| 仅 frequency-only 支持 | 收窄为「winner-frequency identifies task-bearing neurons」，弱化三区复杂规则 |
| medium 支持、full 不支持 | 主文不写分区机制；最多 appendix exploratory |
| per-class 覆盖不均 | 改为「class-wise stable carriers」而非全局 stable 叙事 |

---

## 6. 建议执行顺序（不立即开跑，仅排期）

```
Week 1（判生死，成本低）
  P0-1 / P0-1b / P0-5  ─┐  可在同一 Task1 checkpoint 后验完成
  P0-2 / P0-8          ─┤  无需重训
  P0-6                 ─┘

Week 1–2（稳定性）
  P0-3 seed 1/2        ─── medium 重训 Task1 + 诊断

并行（耗时长）
  P0-4 full            ─── 600 epoch Task1，可与 Week 1 并行

通过 P0 后再开
  P1-7 ~ P1-10         ─── 决定 mechanism 段落能写多深
  P2-11 full           ─── 应用验证
  P2-12 ~ P15          ─── SDPM / reserve，不阻塞分区生死线
```

---

## 7. 记录与归档约定

| 类型 | 路径 | 是否进 git |
| --- | --- | --- |
| 精简指标 JSON | `published_results/diagnostics/` | ✅ |
| 完整 run / checkpoint | `experiments/` | ❌ |
| 本实验列表状态更新 | 本文件 `状态` 列 | ✅ |

每次 P0 实验至少记录：

- run name、seed、git commit
- stable / shared / reserve 数量
- all-200、stable-only、mask-stable accuracy
- random 对照 mean ± std（≥10 seeds）
- natural WTA stable/shared/reserve win %
- 结论标签：**PASS / FAIL / AMBIGUOUS**（对照 §2）

---

## 8. 审阅结论（给作者）

1. **表单足够吗？** — **足够启动「WTA 分区是否值得继续」的决策**；P0 设计逻辑清晰，比直接跑 reserve/SDPM 更对题。  
2. **优先级有问题吗？** — **没有大问题**；建议把 **per-class coverage** 提前，并把 **random 对照限定在 active 池**。  
3. **还要补什么？** — 主要是 **§2 判据**、**P0-1b matched-active random**、**P0-2 双 shuffle 语义**；不必再加很多新实验。  
4. **P2 是否该删？** — 不删，但明确：**P2 证明「有没有用」，P0 证明「是不是真的」**；论文主 claim 能不能立住，只看 P0 + 部分 P1。
